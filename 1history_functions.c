#include "shell.h"

/**
 * _tarikhhy - function that displays the history list.
 *
 * @sal: Structure.
 *
 *  Return: Always 0
 */
int _tarikhhy(info_t *sal)
{
	atb_akam(sal->history);
	return (0);
}
